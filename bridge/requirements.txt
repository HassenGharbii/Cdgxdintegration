# Runs on whichever machine can reach both the simulator's target IP
# (172.24.1.53) and the tx2 FastAPI box. Separate from tx2/requirements.txt —
# this does NOT need to run on the Jetson.
pycomm3==1.2.14
scapy==2.5.0
requests==2.31.0
