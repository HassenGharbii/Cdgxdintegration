POSTGRES_DB=axivis
POSTGRES_USER=axivis
POSTGRES_PASSWORD=axivis
POSTGRES_PORT=5432

# Used by the app (db.py) to connect. Host is the DB container's host machine,
# not "db" — the app runs outside Docker, on the Jetson.
DATABASE_URL=postgresql://axivis:axivis@localhost:5432/axivis
